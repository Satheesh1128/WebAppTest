

name: Test

on:
  push:
    branches: [ "main" ]

jobs:

  build:  
    runs-on: windows-latest
    
    steps:
      - name: Set the value
        id: step_one
        run: |
          echo "action_state=yellow" >> $GITHUB_OUTPUT
          echo "SELECTED_COLOR=green" >> $GITHUB_OUTPUT       
    # outputs:
      # Test: ${{steps.step_one.outputs.action_state}}
      # Test1: ${{steps.step_one.outputs.SELECTED_COLOR}}
      
      - name: Output Data
        id: outputs
        run: |
        env:
          Test: ${{steps.step_one.outputs.action_state}}
          Test1: ${{steps.step_one.outputs.SELECTED_COLOR}}
