

name: Test

on:
  push:
    branches: [ "main" ]

jobs:

  build:  
    runs-on: windows-latest
    
    steps:
      - name: Set the value
        shell: pwsh
        id: step_one
        run: |
          $Service = Get-Date -Format "dd/MM/yyyy HH:mm:ss" 
          write-Output("$Service - Not Running correct number of count")
